apiVersion: v1
kind: Service
metadata:
  name: community-postgres-service
  namespace: default
  labels:
    app: community-postgres
spec:
  type: ClusterIP
  selector:
    app: community-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
