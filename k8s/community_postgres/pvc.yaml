apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: community-postgres-pvc
  namespace: default
  labels:
    app: community-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
