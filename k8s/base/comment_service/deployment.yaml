apiVersion: apps/v1
kind: Deployment
metadata:
  name: comment-service
  namespace: default
  labels:
    app: comment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comment-service
  template:
    metadata:
      labels:
        app: comment-service
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z comment-postgres-service 5432; do
                echo "Waiting for comment-postgres-service:5432..."
                sleep 2
              done
              echo "PostgreSQL is ready."
      containers:
        - name: comment-service
          image: darshanpanzade2006/campus_assist_comment:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: comment-service-config
            - secretRef:
                name: comment-service-secret
          livenessProbe:
            httpGet:
              path: /api/comments/health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/comments/health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
