apiVersion: v1
kind: Service
metadata:
  name: comment-service
  namespace: default
  labels:
    app: comment-service
spec:
  type: ClusterIP
  selector:
    app: comment-service
  ports:
    - name: http
      port: 80
      targetPort: 8003
      protocol: TCP
