apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comment-postgres-pvc
  namespace: default
  labels:
    app: comment-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
