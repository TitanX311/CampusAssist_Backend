apiVersion: v1
kind: Service
metadata:
  name: post-postgres-service
  namespace: default
  labels:
    app: post-postgres
spec:
  type: ClusterIP
  selector:
    app: post-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
