apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: post-postgres-pvc
  namespace: default
  labels:
    app: post-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
