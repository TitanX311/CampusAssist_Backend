apiVersion: v1
kind: Service
metadata:
  name: notification-postgres-service
  namespace: default
  labels:
    app: notification-postgres
spec:
  type: ClusterIP
  selector:
    app: notification-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
