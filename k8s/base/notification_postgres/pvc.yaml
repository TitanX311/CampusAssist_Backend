apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notification-postgres-pvc
  namespace: default
  labels:
    app: notification-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
