apiVersion: v1
kind: Service
metadata:
  name: college-postgres-service
  namespace: default
  labels:
    app: college-postgres
spec:
  type: ClusterIP
  selector:
    app: college-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
