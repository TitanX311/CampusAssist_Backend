apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: search-postgres-pvc
  namespace: default
  labels:
    app: search-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
