apiVersion: v1
kind: Service
metadata:
  name: search-postgres-service
  namespace: default
  labels:
    app: search-postgres
spec:
  type: ClusterIP
  selector:
    app: search-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
