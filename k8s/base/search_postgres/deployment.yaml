apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-postgres
  namespace: default
  labels:
    app: search-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: search-postgres
    spec:
      containers:
        - name: search-postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: search-postgres-secret
          volumeMounts:
            - name: search-postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 5
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: search-postgres-data
          persistentVolumeClaim:
            claimName: search-postgres-pvc
