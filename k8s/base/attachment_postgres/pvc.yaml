apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attachment-postgres-pvc
  namespace: default
  labels:
    app: attachment-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
