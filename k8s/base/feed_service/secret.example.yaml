# Example only — DO NOT COMMIT real values.
# Run: bash k8s/generate-secrets.sh dev
# to generate the real secret from services/feed_service/.env
apiVersion: v1
kind: Secret
metadata:
  name: feed-service-secret
  labels:
    app: feed-service
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://campus_assist_feed:campus_assist_feed@feed-postgres-service:5432/campus_assist_feed_db"
  SECRET_KEY: "REPLACE_ME"
