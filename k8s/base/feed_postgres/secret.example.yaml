# Example only — DO NOT COMMIT real values.
# Run: bash k8s/generate-secrets.sh dev
apiVersion: v1
kind: Secret
metadata:
  name: feed-postgres-secret
  labels:
    app: feed-postgres
type: Opaque
stringData:
  POSTGRES_USER: "campus_assist_feed"
  POSTGRES_PASSWORD: "REPLACE_ME"
  POSTGRES_DB: "campus_assist_feed_db"
