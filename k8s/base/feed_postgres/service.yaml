apiVersion: v1
kind: Service
metadata:
  name: feed-postgres-service
  namespace: default
  labels:
    app: feed-postgres
spec:
  type: ClusterIP
  selector:
    app: feed-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
