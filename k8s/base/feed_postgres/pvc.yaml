apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: feed-postgres-pvc
  namespace: default
  labels:
    app: feed-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
