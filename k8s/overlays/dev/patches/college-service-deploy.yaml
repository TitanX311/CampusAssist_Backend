apiVersion: apps/v1
kind: Deployment
metadata:
  name: college-service
spec:
  template:
    spec:
      containers:
        - name: college-service
          imagePullPolicy: Never
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
