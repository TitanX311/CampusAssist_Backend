apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-postgres
spec:
  template:
    spec:
      containers:
        - name: community-postgres
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
