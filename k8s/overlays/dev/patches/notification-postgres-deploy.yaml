apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-postgres
spec:
  template:
    spec:
      containers:
        - name: notification-postgres
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
