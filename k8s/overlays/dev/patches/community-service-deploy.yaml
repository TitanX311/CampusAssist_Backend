apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
spec:
  template:
    spec:
      containers:
        - name: community-service
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
