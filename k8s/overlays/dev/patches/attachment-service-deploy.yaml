apiVersion: apps/v1
kind: Deployment
metadata:
  name: attachment-service
spec:
  template:
    spec:
      containers:
        - name: attachment-service
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
