apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-service
spec:
  template:
    spec:
      containers:
        - name: post-service
          imagePullPolicy: Never
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
