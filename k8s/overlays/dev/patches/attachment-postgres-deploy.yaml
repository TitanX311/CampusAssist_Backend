apiVersion: apps/v1
kind: Deployment
metadata:
  name: attachment-postgres
spec:
  template:
    spec:
      containers:
        - name: attachment-postgres
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
