apiVersion: v1
kind: Service
metadata:
  name: docs-service
  labels:
    app: docs-service
spec:
  type: ClusterIP
  selector:
    app: docs-service
  ports:
    - name: http
      port: 80
      targetPort: 8005
      protocol: TCP
