# Production environment file for community_service.
# Copy to .env.prod and fill in real values — this file IS committed; .env.prod is NOT.

# Application
APP_NAME=Campus Assist Community Service
DEBUG=False

# Server
HOST=0.0.0.0
PORT=8000

# Database — use a strong password in production
DATABASE_URL=postgresql://<db-user>:<strong-password>@localhost:5432/<db-name>

# JWT — must be identical to SECRET_KEY in auth_service
SECRET_KEY=<same-secret-key-as-auth-service>
